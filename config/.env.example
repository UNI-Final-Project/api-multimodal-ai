# Ejemplo de variables de entorno para la configuración
# Copia este archivo a `config/.env` y rellena los valores reales.

# Clave API de Google (Gemini / GenAI)
GOOGLE_API_KEY=

# Modelo por defecto (opcional)
GEMINI_MODEL=gemini-2.5-flash

# Supabase (cliente de frontend/backend)
NEXT_PUBLIC_SUPABASE_NUTRITION_URL=
NEXT_PUBLIC_SUPABASE_NUTRITION_ANON_KEY=

# Puerto de la aplicación (opcional)
PORT=8000
